# Vercel 部署清单

## ✅ 代码质量检查（已完成）

- [x] TypeScript 编译通过
- [x] ESLint 检查通过
- [x] 全部 52 项断言通过
- [x] Next.js 构建成功
- [x] 无未使用的依赖
- [x] 无硬编码的敏感信息
- [x] 无 SQL 注入风险（全部使用 ORM）
- [x] 无 XSS 风险（React 自动转义，无 dangerouslySetInnerHTML）
- [x] 文件上传有类型和大小验证（4MB，JPG/PNG/WebP）
- [x] 所有写操作都有鉴权（requireAdmin + verifySession）
- [x] 表单输入有服务端验证
- [x] 时间冲突有二次检查（防止并发提交）

## 🔒 安全配置（部署前必做）

### 1. 数据库密码轮换
**当前状态：** ⚠️ 使用开发期间的密码  
**操作步骤：**
1. 登录 Neon 控制台
2. 进入项目 → Roles → `neondb_owner`
3. 点击 Reset password 生成新密码
4. 复制新的连接字符串到 Vercel 环境变量

### 2. 修改管理员密码
**当前状态：** ⚠️ 使用占位符 `change-me-please`  
**操作步骤：**
1. 在 Vercel 项目设置中添加环境变量：
   ```
   ADMIN_PASSWORD=你的强密码
   ```
   建议使用 20+ 字符的随机密码

### 3. 生成 AUTH_SECRET
**当前状态：** ⚠️ 使用开发占位符  
**操作步骤：**
```bash
openssl rand -base64 32
```
复制输出到 Vercel 环境变量：
```
AUTH_SECRET=生成的随机字符串
```

### 4. 配置 Vercel Blob 存储
**操作步骤：**
1. Vercel 项目设置 → Storage → Connect Store
2. 选择 Blob → Create New
3. 连接后会自动添加 `BLOB_READ_WRITE_TOKEN` 环境变量

## 📝 环境变量清单

在 Vercel 项目设置 → Environment Variables 中添加：

```bash
# 数据库（必需）
DATABASE_URL=postgres://xxx@xxx.neon.tech/xxx

# 认证（必需）
ADMIN_PASSWORD=你的强密码
AUTH_SECRET=openssl rand -base64 32 生成

# 存储（必需，连接 Blob 后自动添加）
BLOB_READ_WRITE_TOKEN=vercel_blob_rw_xxx

# 站点配置（可选）
NEXT_PUBLIC_SITE_TITLE=你的直播间名称
NEXT_PUBLIC_ROOM_ID=B站房间号

# 时区配置（可选，默认北京时间）
NEXT_PUBLIC_TIMEZONE=Asia/Shanghai
NEXT_PUBLIC_TIMEZONE_LABEL=北京时间
```

## 🚀 部署步骤

### 方式一：通过 Vercel Dashboard（推荐）

1. **导入 Git 仓库**
   - 登录 Vercel → Add New → Project
   - 选择你的 GitHub/GitLab 仓库
   - Framework Preset 会自动识别为 Next.js

2. **配置环境变量**
   - 在 Environment Variables 区域添加上述所有必需变量
   - Production / Preview / Development 都勾选

3. **连接 Blob 存储**
   - 部署前或部署后都可以
   - Storage → Connect Store → Blob → Create New

4. **部署**
   - 点击 Deploy
   - 首次部署约 2-3 分钟

5. **推送数据库 schema**
   ```bash
   # 本地运行（需要先配置好环境变量）
   npm run db:push
   ```

### 方式二：通过 Vercel CLI

```bash
# 安装 CLI（如果还没装）
npm i -g vercel

# 登录
vercel login

# 部署到生产环境
vercel --prod

# 首次部署会询问项目配置，全部按回车使用默认值即可
```

部署后手动配置环境变量和 Blob 存储。

## 🎯 部署后验证

1. **访问前台页面**
   - 访问 `https://你的域名.vercel.app`
   - 确认页面正常显示，无 JavaScript 错误

2. **测试管理后台**
   - 访问 `https://你的域名.vercel.app/admin`
   - 使用新密码登录
   - 添加一条测试日程
   - 确认前台能看到

3. **测试图片上传**
   - 管理后台 → 添加日程 → 上传海报图
   - 确认能正常上传和显示

4. **测试 B 站直播状态**
   - 配置 `NEXT_PUBLIC_ROOM_ID` 后
   - 开播时访问前台，确认顶部横幅显示

## ⚠️ Vercel Hobby 计划限制

你使用的是免费的 Hobby 计划，注意以下限制：

- ✅ **允许商业使用**（2024 年起政策变更）
- 每月 100GB 带宽
- 每月 1000 次构建分钟数
- 单个函数最多运行 10 秒（无服务器函数）
- Blob 存储 1GB 免费额度

**建议：**
- 图片压缩后再上传（建议 < 500KB）
- 海报图数量控制在 100 张以内
- 如果流量超限，考虑升级到 Pro 计划（$20/月）

## 🔧 常见问题

### 1. 构建失败：找不到环境变量
确保在 Vercel 项目设置中添加了所有必需的环境变量。

### 2. 上传图片失败
确保已连接 Vercel Blob 存储，并且 `BLOB_READ_WRITE_TOKEN` 环境变量存在。

### 3. 登录后立即跳转回登录页
检查 `AUTH_SECRET` 是否正确设置，不能为空。

### 4. 数据库连接失败
检查 `DATABASE_URL` 是否正确，Neon 数据库是否在运行状态。

### 5. 时区显示不对
检查 `NEXT_PUBLIC_TIMEZONE` 和 `NEXT_PUBLIC_TIMEZONE_LABEL` 环境变量。

## 📊 性能优化建议

1. **图片优化**
   - 上传前使用 TinyPNG 等工具压缩
   - 建议海报图尺寸：800×450（16:9）或 800×600（4:3）

2. **数据库优化**
   - Neon 免费层有连接数限制，生产环境考虑升级

3. **缓存策略**
   - Next.js 自动处理静态页面缓存
   - 前台页面每次访问会重新验证数据（revalidate）

## 🎉 部署成功后

1. 将生产环境 URL 分享给观众
2. 绑定自定义域名（可选）：Vercel 项目设置 → Domains
3. 定期备份数据库（Neon 控制台有手动备份功能）
4. 监控 Vercel 使用量：Vercel Dashboard → Usage
