# 图片库功能说明

## 功能概述

海报图现在改用图片库管理，不再手动填写 URL。管理员可以：
- 上传 JPG、PNG、WebP 格式图片（最大 4MB）
- 从已上传的图片中选择作为海报
- 预览当前选中的海报，一键删除

## 使用流程

1. **添加/编辑日程时**：
   - 点击「＋ 填海报图和简介」展开
   - 点击「选择海报图」按钮打开图片库
   
2. **上传新图片**：
   - 在图片库弹窗中，点击虚线框区域选择本地图片
   - 支持 JPG、PNG、WebP 格式，最大 4MB
   - 上传后自动添加到图片库，并选中为当前海报

3. **从图片库选择**：
   - 图片库以网格显示所有已上传的图片
   - 点击任意图片即可选中
   - 鼠标悬停会显示文件名

4. **更换或删除海报**：
   - 已选海报会显示缩略图预览
   - 点击右上角「✕」删除当前海报
   - 点击「换一张」重新打开图片库

## 技术实现

- **存储**：使用 Vercel Blob（免费额度 1 GB）
- **数据库**：新增 `media` 表存储图片元数据（文件名、URL、尺寸等）
- **API**：
  - `POST /api/media/upload` - 上传图片
  - `GET /api/media` - 获取图片库列表（最近 100 张）
- **鉴权**：仅管理员登录后可使用

## 部署配置

### Vercel 平台部署

1. 在 Vercel 项目设置中创建 Blob 存储：
   - 进入项目 → Settings → Storage
   - 点击 Connect Store → 选择 Blob
   - Vercel 会自动添加 `BLOB_READ_WRITE_TOKEN` 环境变量

2. 推送 schema 更新：
   ```bash
   npm run db:push
   ```

### 本地开发

1. 从 Vercel 项目设置中复制 `BLOB_READ_WRITE_TOKEN`
2. 添加到本地 `.env.local` 文件：
   ```
   BLOB_READ_WRITE_TOKEN="vercel_blob_rw_xxxxx"
   ```

## 限制与注意事项

- **文件大小**：单个文件最大 4MB
- **存储空间**：Vercel Blob 免费额度 1 GB
- **文件格式**：仅支持 JPG、PNG、WebP
- **图片管理**：暂不支持删除已上传的图片（可在 Vercel Dashboard 手动管理）
- **推荐尺寸**：宽度 800-1200px 最佳，卡片会自动裁剪为横图显示
